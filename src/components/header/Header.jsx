import React from 'react';
import "./header.scss"

const Header = () => {
    return (
        <header className="header">
            <nav className='logo'>
              <h2>bookapp</h2>
            </nav>
        </header>
    );
};

export default Header;